# Note: Running macOS in Docker requires special licensing and is typically only
# available on macOS hosts. This Dockerfile uses a macOS-like environment.
# For actual macOS testing, consider using GitHub Actions with macos-latest runners.

FROM sickcodes/docker-osx:monterey

# Install Homebrew
RUN /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Add Homebrew to PATH
ENV PATH="/opt/homebrew/bin:${PATH}"

# Install dependencies via Homebrew
RUN brew install git curl

# Install Rust
RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
ENV PATH="/Users/user/.cargo/bin:${PATH}"

# Install Bun
RUN curl -fsSL https://bun.sh/install | bash
ENV PATH="/Users/user/.bun/bin:${PATH}"

# Set up working directory
WORKDIR /app

# Copy project files
COPY . .

# Build the projektwoche-setup tool
RUN cargo build --release --manifest-path rust/projektwoche-setup/Cargo.toml

# Make the binary accessible in PATH
ENV PATH="/app/rust/projektwoche-setup/target/release:${PATH}"

# Set the default command to run installation and verification
CMD ["sh", "-c", "projektwoche-setup install projektwoche && bun scripts/setup-tests.ts projektwoche"]