{
  "name": "projektwoche-monorepo",
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "5.9.2",
    "vercel": "^44.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.19",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format",
    "fmt": "turbo run format",
    "check": "turbo run check",
    "shadcn": "turbo run prowo-ui#shadcn"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@vercel/microfrontends": "^2.0.0"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide",
    "core-js",
    "unrs-resolver"
  ]
}
